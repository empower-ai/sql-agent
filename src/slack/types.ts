export enum Action {
  EditQuery = 'edit_query',
  CancelQueryEditing = 'cancel_query_editing',
  RunEditedQuery = 'run_edited_query',
}

export enum Event {
  OriginalResponse = 'original_response'
}
